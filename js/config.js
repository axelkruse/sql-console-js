/* Modify this file to change the behaviour of SQL Trainer */
/* Same settings maybe modified by appendix to url with # sign */
var srcAssignment = ''; 		/* URL of assignment file. Empty string ommits the "Assignment" Panel */
var showLoadAssign = false; 	/* if showLoadAssign ist true the user will be able to load an external Assignmentfile */
var srcHelp = ''; 				/* URL of help file. Empty string ommits the "Help" Panel */
var srcInitScript = '';			/* URL of a sql script to initialise the database. Empty string ommits initialising */ 
var showLoadScript = false; 	/* if showLoadScript ist true the user will be able to load an external sql script into the editor */
/* End of modification */
/* translation: text of elements set bei javascript */
/* options for selecting frames */
var optConsolFrame = 'SQL Konsole'; 
var optAssignFrame = 'Aufgabenstellung';
var optHistoryFrame = 'Logbuch';
var optStructFrame = 'Datenbank Schema';
var optHelpFrame = 'Hilfe';
var optAboutFrame = 'Über SQL Trainer';

